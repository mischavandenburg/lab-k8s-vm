apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: wordpress-lab
  namespace: argocd
spec:
  project: default
  source:
    chart: wordpress 
    repoURL: https://charts.bitnami.com/bitnami
    targetRevision: 15.2.43
    # helm:
    #   values: |
    #     persistence:
    #       enabled: false
  destination:
    server: "https://kubernetes.default.svc"
    namespace: wordpress
